#include <stdio.h>
#include <stdlib.h>
#include "../Header/menu.h"
#include "../Header/plateau.h"
#include "../Header/deplacement.h"
#include "../Header/init.h"

void main()
{
    int jouer = 0;
    while (menu()) 
    {
        if (jouer)
        {

        }
    }
    system("clear");
}
