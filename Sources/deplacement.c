#include <stdio.h>
#include <stdlib.h>
#include <time.h>

//int de()
int main()
{
    srand(time(NULL));
    return rand() % 6;
}

void deplacer(int distance, pion couleur[0], pion numPion) {
    // Nombre de cases parcourables : 56
    if () {
        
    }
}